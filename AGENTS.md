# 스탁 위젯 개발 지침

이 프로젝트는 Electron 기반 데스크톱 주식 모니터링 위젯이다. 앱은 일반 데스크톱 프로그램이 아니라, 사용자가 다른 프로그램을 보고 있을 때도 항상 화면 위에 떠 있는 작은 위젯으로 동작해야 한다.

## 제품 목표

- 사용자가 선택한 주식 종목의 현재가, 등락률, 갱신 시간을 빠르게 확인할 수 있게 한다.
- 위젯은 항상 다른 창보다 앞에 표시한다.
- 앱은 시스템 트레이를 중심으로 제어한다.
- 실제 종료는 시스템 트레이 메뉴에서만 가능해야 한다.
- 환경설정에서 모니터링할 종목을 검색, 추가, 삭제, 정렬할 수 있어야 한다.

## 핵심 요구사항

1. Electron으로 구현한다.
2. 메인 창은 위젯 형태의 작고 가벼운 UI로 구성한다.
3. 메인 위젯 창은 항상 다른 프로그램보다 앞에 표시한다.
4. 일반 창 닫기 버튼이나 Alt+F4로 앱이 완전히 종료되면 안 된다.
5. 사용자가 앱을 종료하려면 반드시 시스템 트레이의 종료 메뉴를 사용해야 한다.
6. 시스템 트레이 메뉴에는 최소한 다음 항목을 둔다.
   - 위젯 보이기
   - 위젯 숨기기
   - 환경설정
   - 종료
7. 환경설정 화면에서 모니터링 종목을 관리할 수 있어야 한다.
8. 종목 추가 시 종목명 또는 티커로 검색할 수 있어야 한다.
9. 사용자 설정은 앱 재시작 후에도 유지되어야 한다.

## Electron 창 동작

- 메인 위젯 창은 `alwaysOnTop`을 사용한다.
- 위젯 창은 가능하면 프레임 없는 창으로 만든다.
- 사용자가 위젯 위치를 옮길 수 있어야 한다.
- 창 위치와 크기를 저장하고 다음 실행 시 복원한다.
- 창 닫기 이벤트는 종료가 아니라 숨김 처리로 동작해야 한다.
- 실제 종료 플로우는 트레이의 `종료` 메뉴에서만 허용한다.
- 환경설정은 별도 창 또는 명확히 분리된 설정 화면으로 제공한다.

## 시스템 트레이

- 앱 실행 시 시스템 트레이 아이콘을 생성한다.
- 트레이 아이콘 또는 메뉴에서 위젯 표시 상태를 제어할 수 있어야 한다.
- 트레이의 `종료` 항목을 선택했을 때만 앱을 완전히 종료한다.
- 종료 구현 시 `isQuitting` 같은 내부 플래그를 사용해 일반 닫기와 실제 종료를 구분한다.

## 환경설정

- 모니터링할 종목 목록을 추가, 삭제, 정렬할 수 있어야 한다.
- 종목 검색은 종목명과 티커를 모두 지원한다.
- 종목 검색 결과에는 최소한 종목명, 티커, 시장 정보를 표시한다.
- 중복 종목 추가를 방지한다.
- 설정 저장 항목은 다음을 포함한다.
  - 모니터링 종목 목록
  - 위젯 위치
  - 위젯 크기
  - 표시 옵션
  - 데이터 갱신 주기

## 데이터 설계

- 주식 데이터 API는 UI 컴포넌트에 직접 결합하지 않는다.
- 종목 검색 기능과 시세 조회 기능은 서비스 계층에서 분리한다.
- 개발 초기에는 mock 데이터 provider를 먼저 구현한다.
- 실제 API 연동은 provider 인터페이스 뒤에 붙인다.
- API 키가 필요한 경우 Renderer 프로세스에 직접 노출하지 않는다.
- 외부 API 호출은 Main 프로세스 또는 안전하게 제한된 백엔드 계층에서 처리한다.

## UI 지침

- 첫 화면은 랜딩 페이지가 아니라 실제 위젯 UI여야 한다.
- 위젯에는 종목명, 현재가, 등락률, 갱신 시간을 간결하게 표시한다.
- 상승과 하락은 색상과 아이콘으로 구분한다.
- 작은 위젯 안에서 텍스트가 겹치지 않도록 고정된 행 높이와 반응형 제약을 둔다.
- 카드나 장식 위주의 화면보다 반복적으로 확인하기 좋은 밀도 있는 정보 UI를 우선한다.
- 버튼에는 가능한 경우 아이콘을 함께 사용한다.

## 보안 기준

- `nodeIntegration`은 끈다.
- `contextIsolation`은 켠다.
- Renderer와 Main 간 통신은 `preload`와 IPC를 통해 제한적으로 노출한다.
- 앱 종료, 트레이 제어, 파일 시스템 접근, 외부 API 호출은 Renderer에서 직접 수행하지 않는다.
- IPC 채널은 필요한 기능만 명시적으로 제공한다.

## 권장 저장 방식

- 초기 구현에서는 `electron-store` 같은 단순한 로컬 설정 저장소를 우선 고려한다.
- 저장 로직은 UI와 분리한다.
- 설정 스키마를 명확히 두고 기본값을 제공한다.

## 초기 구현 우선순위

1. Electron 앱 기본 구조 구성
2. 항상 위에 뜨는 위젯 창 구현
3. 시스템 트레이 및 트레이 종료 구현
4. 창 닫기 시 숨김 처리
5. 환경설정 화면 추가
6. 종목 검색 및 추가 UI 구현
7. 종목 목록 저장
8. mock 시세 데이터 표시
9. 실제 주식 데이터 API 연동

## 구현 원칙

- 기존 코드 패턴이 생기면 그 패턴을 우선 따른다.
- 기능은 Main, Preload, Renderer의 책임을 명확히 나누어 구현한다.
- 주식 데이터 provider는 교체 가능하게 설계한다.
- Electron 보안 기본값을 약화시키지 않는다.
- 동작 검증 없이 창 종료, 트레이, 저장 동작을 변경하지 않는다.
